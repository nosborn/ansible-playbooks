# host_vars/edgerouter/main
---
ansible_host: "{{ lan_ipv4|ipaddr('address') }}"
ansible_user: "{{ vault_ansible_user }}"
ddns_hostname: "{{ vault_ddns_hostname }}"
ddns_login: "{{ vault_ddns_login }}"
ddns_password: "{{ vault_ddns_password }}"
ddns_protocol: "{{ vault_ddns_protocol }}"
ddns_server: "{{ vault_ddns_server }}"
ddns_service: "{{ vault_ddns_service }}"
dhcpd_dmz_start: "{{ vault_dhcpd_dmz_start }}"
dhcpd_dmz_stop: "{{ vault_dhcpd_dmz_stop }}"
dhcpd_guest_start: "{{ vault_dhcpd_guest_start }}"
dhcpd_guest_stop: "{{ vault_dhcpd_guest_stop }}"
dhcpd_hosts: "{{ vault_dhcpd_hosts }}"
dhcpd_lan_start: "{{ vault_dhcpd_lan_start }}"
dhcpd_lan_stop: "{{ vault_dhcpd_lan_stop }}"
dmz_if: "{{ vault_dmz_if }}"
dmz_ipv4: "{{ vault_dmz_ipv4 }}"
dns_proxy_address: "{{ vault_dns_proxy_address }}"
global_domain: "{{ vault_global_domain }}"
guest_if: "{{ lan_if }}.{{ vault_guest_vlan }}"
guest_ipv4: "{{ vault_guest_ipv4 }}"
guest_vlan: "{{ vault_guest_vlan }}"
# httpd_htpasswd_content: "{{ vault_httpd_htpasswd_content }}"
lan_if: "{{ vault_lan_if }}"
lan_ipv4: "{{ vault_lan_ipv4 }}"
# lan_ipv6: "{{ vault_lan_ipv6 }}"
# lan_rtlabel: "lan"
nameservers: "{{ vault_nameservers }}"
# ntpd_listen_on: "{{ vault_ntpd_listen_on }}"
# ntpd_server: "{{ vault_ntpd_server }}"
# relayd_csp:
#   - "default-src 'none'"
#   - "style-src 'self' 'unsafe-inline'"
#   - "img-src 'self' data:"
#   - "media-src 'self'"
#   - "upgrade-insecure-requests"
#   - "referrer origin"
#   - "report-uri {{ report_uri }}/r/d/csp/enforce"
# relayd_ct:
#   - max-age=60  # TODO: max-age=31536000
#   - report-uri="{{ report_uri }}/r/d/ct/enforce"
#   - enforce
# relayd_sts:
#   - "max-age=31536000"
#   - "includeSubDomains"
#   - "preload"
# report_uri: "https://nosborn.report-uri.com"
# smtpd_max_message_size: "{{ vault_smtpd_max_message_size }}"
# smtpd_relay_host: "{{ vault_smtpd_relay_host }}"
# smtpd_secrets_table: "{{ vault_smtpd_secrets_table }}"
# # sshd_listen_addresses: "{{ vault_sshd_listen_addresses }}"
timezone: "{{ vault_timezone }}"
# # users: "{{ vault_users }}"
# vpn_config: "{{ vault_vpn_config }}"
# vpn_if: "{{ vault_vpn_if }}"
# vpn_password: "{{ vault_vpn_password }}"
# vpn_username: "{{ vault_vpn_username }}"
wan_if: "{{ vault_wan_if }}"
...
