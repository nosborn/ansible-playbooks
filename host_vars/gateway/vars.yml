---
guest_if: "{{ vault_guest_if }}"
guest_ipv4_address: "{{ vault_guest_ipv4_address }}"
guest_ipv6_address: "{{ vault_guest_ipv6_address }}"
iot_if: "{{ vault_iot_if }}"
iot_ipv4_address: "{{ vault_iot_ipv4_address }}"
lan_if: "{{ vault_lan_if }}"
lan_ipv4_address: "{{ vault_lan_ipv4_address }}"
lan2_if: "{{ vault_lan2_if }}"
tunnelbroker_address: "{{ vault_tunnelbroker_address }}"
tunnelbroker_description: "{{ vault_tunnelbroker_description }}"
tunnelbroker_if: "{{ vault_tunnelbroker_if }}"
tunnelbroker_remote_ip: "{{ vault_tunnelbroker_remote_ip }}"
wan_if: "{{ vault_wan_if }}"
wan_ipv4_address: "{{ vault_wan_ipv4_address }}"
