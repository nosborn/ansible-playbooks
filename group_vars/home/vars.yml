---
global_domain: "{{ vault_global_domain }}"
guest_ipv4_subnet: "{{ vault_guest_ipv4_subnet }}"
iot_ipv4_subnet: "{{ vault_iot_ipv4_subnet }}"
isp_domains: "{{ vault_isp_domains }}"
isp_nameservers: "{{ vault_isp_nameservers }}"
lan_ipv4_subnet: "{{ vault_lan_ipv4_subnet }}"
lan2_ipv4_subnet: "{{ vault_lan2_ipv4_subnet }}"
timezone: "{{ vault_timezone }}"
tor_virtual_addr_network: "{{ vault_tor_virtual_addr_network }}"
