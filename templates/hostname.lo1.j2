rdomain 1
inet 127.0.0.1/8
up

!/sbin/route -nq -T1 add -inet 127/8 -reject 127.0.0.1

!/sbin/route -nq -T1 add -inet {{ wan_ipv4|ipaddr('address') }} 127.0.0.1

{% if isp_netblocks is defined %}
{%   for isp_netblock in isp_netblocks|ipv4 %}
#!/sbin/route -nq -T1 add -inet {{ isp_netblock }} 127.0.0.1
{%   endfor %}
{% endif %}
