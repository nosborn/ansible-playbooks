#!/bin/ksh

daemon="/usr/local/bin/imapfilter"
daemon_flags="-l /home/{{ imapfilter_rc_user }}/.imapfilter/imapfilter.log"
daemon_user="{{ imapfilter_rc_user }}"

. /etc/rc.d/rc.subr

rc_bg=YES
rc_reload=NO

rc_cmd $1
