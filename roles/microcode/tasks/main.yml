---
- name: install package
  ansible.builtin.package:
    name: intel-ucode
    state: present
  register: microcode_result
  until: microcode_result is not failed
  when:
    - ansible_architecture == 'x86_64'
    - "'GenuineIntel' in ansible_processor"
