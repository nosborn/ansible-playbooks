## Allow wheel by default.
#permit keepenv :wheel

permit nopass setenv { ENV PS1 SSH_AUTH_SOCK } :wheel as root

permit nopass keepenv root as root

#permit nopass keepenv _openvpn as root cmd /sbin/ifconfig
#permit nopass keepenv _openvpn as root cmd /sbin/route
