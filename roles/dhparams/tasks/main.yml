# roles/dhparams/tasks/main
---
- command: /usr/bin/openssl dhparam -out {{ dhparams_file }} 2048
  args:
    creates: "{{ dhparams_file }}"

- file: path={{ dhparams_file }} owner=root group=wheel mode=0444

- fetch: dest=backup fail_on_missing=yes src={{ dhparams_file }}
...
