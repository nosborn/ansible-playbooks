---
- name: install package
  ansible.builtin.apt:
    deb: https://get.gravitational.com/teleport_{{ teleport_version }}_amd64.deb
    install_recommends: false
    state: present

- name: enable and start service
  ansible.builtin.systemd:
    name: teleport.service
    enabled: true
    state: started
