#!/bin/ksh

daemon="/usr/local/sbin/blocky"
daemon_flags="--config /etc/blocky/config.yml"
daemon_logger="daemon.info"
daemon_user="_blocky"

. /etc/rc.d/rc.subr

pexp="${daemon}.*"
rc_bg=YES

rc_cmd $1
