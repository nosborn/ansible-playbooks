---
- name: configure periodic system maintenance
  ansible.builtin.template:
    src: "{{ item }}.j2"
    dest: /etc/{{ item }}
    owner: root
    group: wheel
    mode: 0444
    validate: ksh -n %s
  loop:
    - daily.local
    - weekly.local
