# roles/periodic/tasks/main
---
- name: periodic system maintenance scripts
  copy:
    src: "{{ item }}"
    dest: /etc/{{ item }}
    owner: root
    group: wheel
    mode: 0444
    validate: /bin/ksh -n %s
  with_items:
    - daily.local
