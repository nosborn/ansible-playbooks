# roles/unbound/handlers/main
---
- name: reload unbound
  service:
    name: "{{ unbound_service_name }}"
    state: reloaded

- name: restart unbound
  service:
    name: "{{ unbound_service_name }}"
    state: restarted
