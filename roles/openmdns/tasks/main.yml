# roles/openmdns/tasks/main
---
- name: install package
  openbsd_pkg: name=openmdns state=latest

- name: enable multicast DNS/DNS-SD daemon
  service:
    name=mdnsd arguments="-w {{ lan_if }}"
    enabled={{ mdnsd_enabled }} state={{ mdnsd_state }}
...
