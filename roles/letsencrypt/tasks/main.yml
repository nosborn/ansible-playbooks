# roles/letsencrypt/tasks/main
---
- copy:
    content="{{ letsencrypt_account_key }}" dest=/etc/acme/privkey.pem
    owner=root group=wheel mode=0400

- name: ACME client configuration file
  template:
    src=acme-client.conf.j2 dest=/etc/acme-client.conf
    owner=root group=wheel mode=0440
...
