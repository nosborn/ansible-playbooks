# /etc/acme/letsencrypt-privkey.pem
# /etc/daily.local
# /etc/kbdtype
/etc/mail/secrets
# /etc/rc.firsttime
# /etc/rc.sysmerge
# /etc/ssl/*.crt
# /etc/ssl/*.chain.pem
# /etc/ssl/*.fullchain.pem
# /etc/ssl/private/*.key
/obsd.rd
/obsd.sp
# /var/unbound/db/root.key
/var/unbound/etc

@include "/etc/changelist"
