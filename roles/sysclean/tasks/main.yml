# roles/sysclean/tasks/main
---
- name: install package
  openbsd_pkg: name=sysclean state=installed

- template:
    src=sysclean.ignore.j2 dest=/etc/sysclean.ignore
    owner=root group=wheel mode=0444
...
