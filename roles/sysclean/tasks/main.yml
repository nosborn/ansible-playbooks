---
- name: install sysclean
  ansible.builtin.package:
    name: sysclean
    state: present

- name: configure sysclean exclusions
  ansible.builtin.template:
    src: sysclean.ignore.j2
    dest: /etc/sysclean.ignore
    owner: root
    group: wheel
    mode: 0444
