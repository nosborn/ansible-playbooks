# roles/httpd/tasks/main
---
#- copy:
#    content="{{ nginx_dhparam }}" dest=/etc/ssl/dhparam.pem
#    owner=root group=wheel mode=0444
#  notify: restart nginx

#- file:
#    path=/var/www/htdocs/{{ item }} state=directory
#    owner=root group=daemon mode=0755
#  with_items:
#    - osborn.io

#- copy:
#    src=html dest=/var/www
#    owner=root group=daemon mode=0644 directory_mode=0755

- template:
    src=httpd.conf.j2 dest=/etc/httpd.conf
    owner=root group=wheel mode=0444
    validate='/usr/sbin/httpd -f %s -n'
  notify: reload httpd

- service: name=httpd enabled=yes state=started
...
