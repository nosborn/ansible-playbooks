# roles/yamllint/tasks/main
---
- name: OS-specific variables
  include_vars: "{{ ansible_distribution }}.yml"

- name: install package
  package:
    name: "{{ yamllint_package_name }}"
    state: present
  register: yamllint_package_result
  until: yamllint_package_result is succeeded
...
