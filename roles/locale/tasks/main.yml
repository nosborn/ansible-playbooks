# roles/locale/tasks/main
---
- copy:
    content="{{ locale_kbdtype }}\n"
    dest=/etc/kbdtype
    owner=root group=wheel mode=0444

- name: set localtime link
  file:
    src: /usr/share/zoneinfo/{{ locale_timezone }}
    state: link
    dest: /etc/localtime
    force: yes
...
