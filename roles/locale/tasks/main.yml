# roles/locale/tasks/main
---
- file:
    content="{{ locale_kbdtype }}\n" dest=/etc/kbdtype
    owner=root group=wheel mode=0444

- name: set localtime link
  file:
    src=/usr/share/zoneinfo/{{ locale_timezone }} dest=/etc/localtime state=link
    force=yes
...
