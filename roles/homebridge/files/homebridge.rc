#!/bin/ksh

daemon="/usr/local/bin/homebridge"
daemon_flags="-Q -T -U /var/homebridge"
daemon_logger="daemon.info"
daemon_user="_homebridge"

. /etc/rc.d/rc.subr

pexp="node: homebridge"

rc_bg=YES
rc_reload=NO

rc_cmd $1
