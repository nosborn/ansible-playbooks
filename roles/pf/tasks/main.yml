---
- name: install configuration
  ansible.builtin.template:
    src: pf.conf.j2
    dest: /etc/pf.conf
    owner: root
    group: wheel
    mode: 0400
    validate: /sbin/pfctl -f %s -n
  notify: reload pf
