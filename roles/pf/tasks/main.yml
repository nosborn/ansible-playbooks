---
- name: configuration pf
  ansible.builtin.copy:
    content: "{{ pf_rules }}"
    dest: /etc/pf.conf
    owner: root
    group: wheel
    mode: 0400
    validate: pfctl -f %s -n
  notify: reload pf
